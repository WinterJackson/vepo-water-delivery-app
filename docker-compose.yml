version: "3.8"

services:
  api:
    build: ./BackendAPI
    ports:
      - "8000:8000"
    env_file:
      - ./BackendAPI/.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
